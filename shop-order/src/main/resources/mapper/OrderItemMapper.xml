<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.shj.shop.order.mapper.OrderItemMapper">
    <insert id="insert">
        INSERT INTO `spring-boot-action`.shop_order_item
        (order_id, pro_id, pro_name, pro_price, `number`)
        VALUES(#{orderId}, #{proId}, #{proName}, #{proPrice}, #{number});
    </insert>

</mapper>
